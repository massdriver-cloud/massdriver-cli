schema: draft-07
name: aws-vpc
description: Something
type: bundle
access: public
params:
  required: ["name"]
  properties:
    name:
      type: string
      title: Name
    age:
      type: integer
      title: Age
connections:
  required: ["default"]
  properties:
    default:
      type: string
      title: Default credential

artifacts:
