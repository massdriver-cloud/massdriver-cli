schema: draft-07
name: "renders-successfully"
description: "Renders an application config & subdirectories"
source_url: github.com/YOUR_ORG/renders-successfully
access: private
type: application

params:
  properties:
    hostname:
      type: string
    app_env:
      type: string

connections:
  properties: {}

ui:
  ui:order:
    - hostname
    - app_env
