schema: draft-07
name: renders-successfully
description: Renders an application config & subdirectories
ref: github.com/YOUR_ORG/renders-successfully
access: private
type: application

params:
  properties:
    hostname:
      type: string
    app_env:
      type: string

dependencies:
  database:
    type: massdriver/postgresql-authentication
    required: true

ui:
  ui:order:
    - hostname
    - app_env
