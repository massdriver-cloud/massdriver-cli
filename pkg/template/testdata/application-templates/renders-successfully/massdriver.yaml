schema: draft-07
name: "{{ name }}"
description: "{{ description }}"
source_url: github.com/YOUR_ORG/{{ name }}
access: private
type: application

params:
  properties:
    hostname:
      type: string
    app_env:
      type: string

connections:
  properties: {}

ui:
  ui:order:
    - hostname
    - app_env
