schema: draft-07
uuid: deadbeef-need-to-generate
title: Retool
description: Self-hosted retool
provisioner: terraform
access: private
type: retool
params:
  domain_name:
    chartValuePath: ingress.hostName
    required: true
    title: DNS Name
    type: string
  license_key:
    chartValuePath: config.licenseKey
    required: true
    title: License Key
    type: string
connections:
  required:
  - database
  - kubernetes_cluster
  - topic
  properties:
    database:
      ref: massdriver/rdbms-connection
    kubernetes_cluster:
      ref: massdriver/kubernetes-cluster
    queue:
      ref: massdriver/aws-sqs-queue
    topic:
      ref: massdriver/aws-sns-topic
